# Homework 04: Authentication & Security 🔐

Це фінальна частина бекенд-додатку для керування контактами, де реалізовано повноцінну систему реєстрації, логіна та захисту маршрутів за допомогою **JWT (JSON Web Tokens)**.

## 🚀 Основні функції
- **Реєстрація користувачів**: Шифрування паролів за допомогою `bcrypt`.
- **Автентифікація**: Логін користувача та видача Access/Refresh токенів.
- **Приватні маршрути**: Доступ до контактів мають лише авторизовані користувачі.
- **Робота з базою даних**: MongoDB (через Mongoose).
- **Валідація**: Перевірка вхідних даних за допомогою `Joi`.

## 🛠 Технологічний стек
- **Node.js** (v22+)
- **Express.js** (фреймворк сервера)
- **MongoDB Atlas** (база даних)
- **Mongoose** (ODM для роботи з базою)
- **JWT** (авторизація через токени)
- **Bcrypt** (хешування паролів)

## ⚙️ Налаштування оточення (Environment Variables)
Для запуску проекту локально, створи файл `.env` у корені та додай наступні змінні:
```env
PORT=3000
MONGODB_URL=твоє_посилання_на_mongodb
JWT_SECRET=твій_секретний_ключ
